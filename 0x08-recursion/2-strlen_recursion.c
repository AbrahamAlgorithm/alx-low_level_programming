#include "main.h"
#include <stdio.h>
/**
 * _strlen_recursion : returns the lenght of a string
 *
 * Return : 0
 */
int main(void)
{
	int n = 0;

	n = _strlen_recursion("Corbin Coleman");
	printf("%d\n", n);
	return (0);
}
