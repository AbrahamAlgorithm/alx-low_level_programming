#include <stdio.h>
/**
 * a program that print its name
 * followed by a new line
 * Return : 0
 */
int main(int argc, char *argv[])
{
	(void)argc;
       	printf("%s\n", argv[0]);
       	return (0);
}

